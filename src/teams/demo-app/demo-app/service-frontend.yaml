apiVersion: v1
kind: Service
metadata:
  name: demo-app-frontend
  namespace: demo-app
spec:
  selector:
    app: demo-app-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  type: ClusterIP
