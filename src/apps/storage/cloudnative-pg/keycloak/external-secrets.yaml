apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-db-user-es
  namespace: storage
spec:
  refreshInterval: "1h"
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: keycloak-db-user
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          app: keycloak-db-cluster
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: platform/storage/cnpg/keycloak
        property: username
    - secretKey: password
      remoteRef:
        key: platform/storage/cnpg/keycloak
        property: password