apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: deployment-templates
  namespace: ai
spec:
  serviceAccountName: workflow-executor

  templates:
    - name: update-gitops
      inputs:
        parameters:
        - name: model_name
        - name: prod_version
        - name: training_image
      volumes:
      - name: git-secret
        secret:
          secretName: gitops-repo
      script:
        image: alpine/git:latest
        command: [sh]
        volumeMounts:
        - name: git-secret
          mountPath: /mnt/secrets
          readOnly: true
        source: |
          set -e

          # Install yq
          apk add --no-cache yq

          # Setup SSH
          mkdir -p ~/.ssh
          cat /mnt/secrets/sshPrivateKey > ~/.ssh/id_ed25519
          echo "" >> ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan github.com >> ~/.ssh/known_hosts

          MODEL_NAME="{{inputs.parameters.model_name}}"
          PROD_VERSION="{{inputs.parameters.prod_version}}"
          TRAINING_IMAGE="{{inputs.parameters.training_image}}"
          NEW_URI="models:/prod.${MODEL_NAME}/${PROD_VERSION}"

          # Strip whitespace
          MODEL_NAME=$(echo "$MODEL_NAME" | tr -d '\n\r\t ')

          echo "📦 Deployment Request:"
          echo "   Model: ${MODEL_NAME}"
          echo "   Version: v${PROD_VERSION}"
          echo "   Image: ${TRAINING_IMAGE}"
          echo ""

          git clone git@github.com:OpenCloudHub/gitops.git /tmp/gitops
          cd /tmp/gitops

          SERVICE_FILE="src/team-apps/ai/ray/${MODEL_NAME}-service.yaml"

          # Get current values
          CURRENT_IMAGE=$(yq eval '.spec.rayClusterConfig.headGroupSpec.template.spec.containers[0].image' "$SERVICE_FILE")
          CURRENT_URI=$(yq eval '.spec.serveConfigV2' "$SERVICE_FILE" | yq eval '.applications[0].deployments[0].user_config.model_uri' -)

          echo "📊 Current State:"
          echo "   Image: ${CURRENT_IMAGE}"
          echo "   Model: ${CURRENT_URI}"
          echo ""

          CHANGED=false

          # Update image if different
          if [ "$CURRENT_IMAGE" != "$TRAINING_IMAGE" ]; then
            echo "✏️  Updating image: ${CURRENT_IMAGE} → ${TRAINING_IMAGE}"
            yq eval -i ".spec.rayClusterConfig.headGroupSpec.template.spec.containers[0].image = \"${TRAINING_IMAGE}\"" "$SERVICE_FILE"
            yq eval -i ".spec.rayClusterConfig.workerGroupSpecs[0].template.spec.containers[0].image = \"${TRAINING_IMAGE}\"" "$SERVICE_FILE"
            CHANGED=true
          else
            echo "✓ Image unchanged"
          fi

          # Update model URI if different
          if [ "$CURRENT_URI" != "$NEW_URI" ]; then
            echo "✏️  Updating model: ${CURRENT_URI} → ${NEW_URI}"
            if yq eval '.spec.serveConfigV2' "$SERVICE_FILE" | grep -q "user_config:"; then
              yq eval -i '(.spec.serveConfigV2 | from_yaml | .applications[0].deployments[0].user_config.model_uri) = "'"${NEW_URI}"'"' "$SERVICE_FILE"
            else
              yq eval -i '(.spec.serveConfigV2 | from_yaml | .applications[0].deployments[0].user_config) = {"model_uri": "'"${NEW_URI}"'"}' "$SERVICE_FILE"
            fi
            CHANGED=true
          else
            echo "✓ Model URI unchanged"
          fi

          echo ""

          if [ "$CHANGED" = false ]; then
            echo "✅ Already at target state - no changes needed"
            exit 0
          fi

          # Commit and push
          git config user.name "Argo Workflow"
          git config user.email "workflow@opencloudhub.org"
          git add "$SERVICE_FILE"
          git commit -m "🤖 Deploy ${MODEL_NAME} v${PROD_VERSION}"
          git push

          echo ""
          echo "✅ GitOps repo updated! ArgoCD will sync the changes."
