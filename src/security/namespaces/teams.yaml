---
apiVersion: v1
kind: Namespace
metadata:
  name: demo-app
  labels:
    istio.io/dataplane-mode: ambient
# TODO: add demo db secret and change in deployment
---
# Not part of mesh for faster serving, no need for secure communication with other pods in cluster
apiVersion: v1
kind: Namespace
metadata:
  name: models
  labels:
    opencloudhub.org/secret-minio: "true"
